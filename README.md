To Run this app locally, clone this repo and just run docker compose up.

To Deploy this app on AWS ECS. Go to terraform folder and use the below commands to build, review and execute.

terraform init
terraform plan
terraform apply